/**
 * Myaira の営業プレイブック(営業手法の前提)。
 *
 * AI が「次アクション」を提案するときの前提として渡す。ステージごとの狙いは
 * lib/sales/types.ts の SALES_STAGES.description が担い、ここでは全体の方針・型を持つ。
 * これは営業手法(コーチングの前提)であり、課金システムのトライアル期間とは別。
 */
import { SALES_STAGES } from "./types";

export const SALES_PLAYBOOK = `
# Myaira 営業プレイブック(運営 = 株式会社Revorise が Myaira を人材紹介エージェントに売る際の型)

## 基本フロー
1. 営業 Zoom を2回行い、2回目でテスト導入の意思決定を取りにいく。
2. テスト導入が決まったら、その Zoom の終わり際に、その場で Myaira アカウントを即作成する
   (熱量が高いうちにアカウントまで作りきる)。
3. 初回オンボーディングで NDA と利用規約に署名してもらう。署名できれば、その場で CSV
   (求職者名簿)を受領して取り込む(= その場CSV。最速の理想経路)。
4. CSV をその場で渡すことに抵抗がある会社もある。その場合は 2〜3日後にフォローミーティングを
   設定し、そこで必ず CSV 取込作業を「一緒にやる」形で先導する(= 2〜3日後CSV(先導))。
   CSV が入らないとテストが始まらないので、ここを絶対に前に進める。
5. CSV 取込後は 2週間のトライアルとする。旧来の「1ヶ月・週次ミーティング」はハマらなかった
   ため、短く区切って熱量を保つ。
6. トライアルの手応えを踏まえ、本契約(有料プラン)を提案する。検討・保留はフォローで温める。

## コーチングの原則
- 事実(議事録に書かれた発言・状況)だけを根拠にする。議事録に無い事実を創作しない。
- 次アクションは「具体的・すぐ実行できる・期限つき」で示す。
- ボトルネック(特に CSV 取込・意思決定)を前に進めることを最優先にする。
- 相手の懸念・反論には、次回どう返すかまで具体化する。

## ステージ一覧(現在地の把握用)
${SALES_STAGES.map((s, i) => `${i + 1}. ${s.label} … ${s.description}`).join("\n")}
`.trim();
