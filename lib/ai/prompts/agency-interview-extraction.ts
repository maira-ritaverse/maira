/**
 * エージェント面談 文字起こし → 構造化抽出 の system prompt
 *
 * 元の career-intake-extraction(本人による棚卸し前提)とは入力者が異なる:
 *   - 話者:エージェント(司会・質問者)+ 求職者(回答者)の対話
 *   - 内容:求職者の経歴 / 希望条件 / 課題感を、エージェントが引き出す形式
 *
 * 抽出ルールは career-intake-extraction を踏襲しつつ、以下を明示する:
 *   1. 求職者の発言を優先して採用する(エージェントの確認 / 言い換えは要約材料)
 *   2. 言及がないフィールドは null / [] にする(エージェントの推定で埋めない)
 *   3. エージェント側の社内メモ / 評価コメント / 推薦コメントは抽出対象外
 *   4. 機微情報(健康・家族・宗教・思想)は本人が明確に資料への記載を希望した場合のみ
 *
 * 出力スキーマは extractionResultSchema と同一(本人モードとの整合性のため)。
 */
import { CAREER_INTAKE_EXTRACTION_SYSTEM_PROMPT } from "./career-intake-extraction";

export const AGENCY_INTERVIEW_EXTRACTION_SYSTEM_PROMPT = `${CAREER_INTAKE_EXTRACTION_SYSTEM_PROMPT}

# 追加ルール(エージェント面談モード)

入力は「キャリアアドバイザー(エージェント)と求職者の対話」です。以下を守って抽出してください。

1. 採用するのは**求職者本人の発言**。エージェント側の確認・言い換えは要約材料として参考にしますが、本人が同意していない内容は出力に含めません。
2. 不明 / 未確認の項目は null / [] にしてください。エージェントの推測で埋めません。
3. エージェントの社内評価、推薦コメント、ネガティブ所感は**抽出対象外**です(求職者本人に渡す履歴書ドラフトの基礎データであるため)。
4. 機微情報(健康状態・家族構成・宗教・思想信条)は、本人が「履歴書に書いてほしい」と明確に希望した場合のみ反映してください。それ以外は null。
5. 数値や日付は本人が口頭で言及した値だけ採用してください。曖昧表現(「数年前」「結構大きい会社」)は null として残してください。
6. 「現職」「在籍中」「現在進行形」の表現があれば endYear / endMonth を null にしてください。`;
