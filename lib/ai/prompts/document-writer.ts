import type { DocumentType } from "@/lib/documents/types";
import type { CareerProfile } from "@/lib/career/profile-schema";

/**
 * 各書類タイプ別のシステムプロンプト
 *
 * 共通方針:
 * - キャリア棚卸し結果を入力として、書類を生成
 * - 一般論を避け、ユーザー固有の経験を反映
 * - 「素晴らしい」等の自画自賛を入れない
 * - 簡潔で読みやすい日本語
 */

const COMMON_RULES = `
# 共通ルール

1. **棚卸し結果に書かれた情報のみ使う**:
   - 棚卸し結果にない経歴・スキルを勝手に作らない
   - 「○○な経験があれば書いてください」と本人に任せるべき情報は、生成しない

2. **自画自賛は避ける、ただし事実+成果は具体的に書く**:
   悪い例:「素晴らしい成果を残した」「卓越した能力を発揮した」
   良い例:「○○の課題に対し、××を実施することで△△を達成」
   - 数値があれば数値を使う
   - 数値がなければ捏造せず、定性的に書く

3. **採用担当者の視点で書く**:
   - 採用担当者は短時間で読む。要点を先に。
   - 読みやすい改行・段落分け
   - 抽象論より具体的なエピソード

4. **空欄を作らない**:
   - 棚卸し結果に言及があるなら、必ず反映する
   - 言及がない情報は「(別途記入)」と明記する

5. **求人情報がない場合**:
   - 業界・職種を仮定して書く(架空の企業名は書かない)
   - 「業界・職種に応じて調整してください」とは書かない
   - 汎用的に「成果が伝わる」表現に絞る`;

export const DOCUMENT_PROMPTS: Record<DocumentType, string> = {
  motivation: `あなたは志望動機作成の専門家です。
キャリア棚卸し結果と求人情報に基づいて、400-450字の志望動機を作成します。

# 必ず守ること
- **400-450字の範囲を厳守**(これより短い/長いはNG)
- 自然な文章で、見出しや箇条書きは使わない
- 「貴社」「御社」の使い分け(書面なら「貴社」)

# 構成(本文は段落で分けるが、見出しは使わない)

## 要素1:「なぜこの会社・職種に興味を持ったか」(80-120字)
- 求人情報の中の具体的要素に言及
- 「成長中の業界だから」等の一般論は避ける

## 要素2:「自分のどの経験/強みが活きるか」(150-200字)
- 棚卸し結果の strengths から関連性の高いものを選ぶ
- 具体的なエビデンスを含める

## 要素3:「入社後にやりたいこと/貢献したいこと」(120-180字)
- 棚卸し結果の wants / values を反映
- 業務に紐づく具体性

${COMMON_RULES}

# 求人情報がない場合
業界・職種を仮定して書く。架空の企業名は使わない。`,

  self_pr: `あなたは自己PR作成の専門家です。
キャリア棚卸し結果(必要に応じて求人情報)に基づいて、300-350字の自己PRを作成します。

# 必ず守ること
- **300-350字の範囲を厳守**
- 自然な文章で、見出しや箇条書きは使わない
- 1つの強みに焦点を絞る(複数の強みを羅列しない)

# 構成

## 要素1:強みの宣言(50-80字)
- 「私の強みは○○です」と明確に
- 棚卸し結果の strengths から最も訴求力の高いものを選ぶ
- 求人情報があれば、関連性の高いものを選ぶ

## 要素2:エピソード(150-200字)
- 具体的な状況・行動・結果
- 数字があれば数字で(なければ定性的に)

## 要素3:貢献の宣言(80-120字)
- その強みを活かして、どう貢献するか
- 入社後の貢献に繋げる

${COMMON_RULES}

# 注意点
- 「コミュニケーション能力」等の曖昧な強みは避ける、具体的に
- 1つの強みに絞ることで深掘りする`,
};

/**
 * 書類生成用のプロンプトコンテキスト
 *
 * system はタイプ別の指示、prompt は棚卸し結果+求人情報を含む入力データ。
 * モデルへは「system に役割、prompt にデータ」の役割分担で渡す。
 */
export function buildDocumentPrompt(params: {
  type: DocumentType;
  profile: CareerProfile;
  jobInfo?: string;
  customInstructions?: string;
}): { system: string; prompt: string } {
  const system = DOCUMENT_PROMPTS[params.type];

  const promptParts: string[] = [
    "以下は、ユーザーのキャリア棚卸し結果です。",
    "",
    "【キャリア棚卸し結果】",
    JSON.stringify(params.profile, null, 2),
  ];

  if (params.jobInfo) {
    promptParts.push("", "【求人情報】", params.jobInfo);
  }

  if (params.customInstructions) {
    promptParts.push("", "【追加の指示】", params.customInstructions);
  }

  promptParts.push("", "上記を踏まえて、書類を生成してください。");

  return {
    system,
    prompt: promptParts.join("\n"),
  };
}
